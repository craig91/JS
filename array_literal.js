var colors;
colors = ['white', 'black', 'custom'];

var el = document.getElementById('colors');
el.textContent = colors[0];


// This is an array literal. The values are assigned to the array inside a pair 
// of square brackets and each value is seperated by a comma. The values in the array
// do not need to be the same data type, so you can store a string,number and a boolean 
// in the same array.