function sayHello() {
	document.write('Hello!');
	}

/* To create a function, you give it a name and then write the statements needed to achieve
 its task inside the curly braces.

 You declare a function using the function key word
 You give the funtion a name (sometimes called an identifier) followed by parentheses
 in this case sayHello() is the name of the funtion.

 The statements that perform the task sit inside a code block
 sayHello(){
	document.write('Hello')
 }
 The code block is represented by the curly braces while the statements to be executed
 sit inside of it.